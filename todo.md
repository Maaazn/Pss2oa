- [x] إعادة إنتاج سبب استدعاء `waitForCore()` قبل بدء `initCore()` في متصفح iPhone.
- [x] جعل اختيار ISO ينتظر نقطة تهيئة موثوقة بدلاً من رمي خطأ مبكر.
- [x] إبقاء زر ISO متاحاً بعد انتقال واجهة عداد الإطارات من `ready` إلى `running`.
- [ ] بناء النسخة، دفعها إلى GitHub، والتحقق من عملية Cloudflare Pages وحزمة الإنتاج.
- [ ] إعادة فحص واجهة الإنتاج وإبلاغ المستخدم برابط إصدار جديد للاختبار.
