- [x] إعادة إنتاج سبب استدعاء `waitForCore()` قبل بدء `initCore()` في متصفح iPhone.
- [x] جعل اختيار ISO ينتظر نقطة تهيئة موثوقة بدلاً من رمي خطأ مبكر.
- [x] إبقاء زر ISO متاحاً بعد انتقال واجهة عداد الإطارات من `ready` إلى `running`.
- [x] بناء النسخة، دفعها إلى GitHub، والتحقق من عملية Cloudflare Pages وحزمة الإنتاج.
- [x] إعادة فحص واجهة الإنتاج وإبلاغ المستخدم برابط إصدار جديد للاختبار.
- [x] مقارنة ترويسات HTML وPlay.js وPlay.wasm مع User-Agent لسفاري iPhone بين Pss2oa وPlay!.js الرسمي.
- [x] التحقق من أن استجابة Cloudflare لا تغيّر مسار worker أو MIME أو العزل عند طلب Safari.
- [ ] اتخاذ قرار مبني على الدليل: إبقاء Cloudflare أو نقل اختبار التشغيل إلى استضافة بديلة.
- [ ] اعتماد نجاح Play!.js الرسمي على iPhone كخط أساس وإزالة فرضيات فشل Safari العامة من التشخيص.
- [ ] عرض سبب فشل `initCore` الأصلي بدلاً من استبداله برسالة انتظار ISO الثانوية.
- [ ] فك PlayCoreSmoke.zip وفحص ملفات البدء وتهيئة Play! بأمان.
- [ ] مقارنة تكامل PlayCoreSmoke مع `playCore.ts` و`discImageDevice.ts` في Pss2oa.
- [x] إضافة اختبار smoke مستقل إلى نفس نشر Cloudflare لعزل الاستضافة عن واجهة Pss2oa.
- [ ] إثبات اسم الحزمة والالتزام اللذين ينفذهما Safari في جلسة الخطأ الحالية.
- [ ] تسجيل نقاط دخول `initCore` ونتيجة `crossOriginIsolated` في الواجهة الحية.
- [ ] حذف الاعتماد على سباق React Effect ببدء النواة صراحة من مسار اختيار ISO عند الحاجة.
